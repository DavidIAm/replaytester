
// basic imports
var events = require('events');
var fs = require('fs');
var myidentifier = require('json-schema');
var roottype = {};

// for us to do a require later
module.exports = identifier;

function identifier( secret ) {
	events.EventEmitter.call(this);
	this.identify = function (identity) {
		
	};
}

// inherit events.EventEmitter
identifier.super_ = events.EventEmitter;
identifier.prototype = Object.create(events.EventEmitter.prototype, {
		constructor: {
				value: identifier,
				enumerable: false
		}
});

