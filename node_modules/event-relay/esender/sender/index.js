/*
 * POST to the cat
 */

var sys = require('sys');
var fs = require('fs');
//fs.readFile('MantaEvent.schema', function( err, data ) {

var validate = require('commonjs-utils/json-schema').validate;
var message = require('');

//fs.readFile('MantaEvent.schema', function( err, data ) {
//		if(err) throw err;
//		var schema = data;
//	} );

exports.persist = function(req, res){
//	message.validate();
	rabbit.send(req.body);
  res.send({ "success":true });
};

exports.transit = function(req, res){
//	message.validate();
	rabbit.send({ });
  res.send({ });
};

